# DISCO — Python dependencies (tested versions)
#
# torch/torchvision are the CUDA 11.8 builds; install them from the official
# PyTorch index (the line below points pip at it). For a CPU-only or different
# CUDA setup, replace the two "+cu118" pins with the matching build from
# https://pytorch.org/get-started/previous-versions/ (PyTorch 2.0.1).

--extra-index-url https://download.pytorch.org/whl/cu118

torch==2.0.1+cu118
torchvision==0.15.2+cu118
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.2
scikit-image==0.25.0
opencv-python==4.8.0
matplotlib==3.8.2
pandas==1.5.1
Pillow==11.1.0
tqdm==4.66.1
